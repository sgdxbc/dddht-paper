\subsection{System Model}
We consider a decentralized system in which nodes can freely join and leave.
Each node maintains finite local storage, and the entire set of participants collectively implements a storage system.
\sys exposes a \textit{synchronous} object storage interface with three API calls: \query, \store, and \del.
\store takes an object, and returns an ID when the object is successfully stored.
\del takes an object ID and removes the object from the system.
\query takes an object ID and returns the object if it exists in the system.
\lijl{Should we clarify that object ID is the hash of the object here?}
\sgd{If we do clarify, we can also mention verifiable result by comparing hashes.}
\sys does not provide an explicit interface to update an existing object.
Update can be done by issuing a \del followed by a \store to overwrite the entire object.
Note that in this case, the \store will return a different object ID for the updated object.
Consistently mapping client objects to \sys issued IDs is out of the scope of the paper.

\sys guarantees \textit{data durability}.
Once a \store successfully returns, all subsequent \query{}s for the generated ID return the original object.
\del is best-effort in \sys: After a completed \del, a \query may still return the object.
Combined with the fact that \sys does not support explicit object update, data durability also implies linearizability~\cite{linearizability} of client \query{}s and \store{}s.
\lijl{Need to be more careful with this claim}

We assume two types of failures that relate to data losses -- storage device failures and Byzantine failures.
Storage device failures lead to unrecoverable data loss of the entire or part of a device.
They are stochastic events: The failure probability of a device is modeled after annualized failure rate (AFR), which depends on factors such as device makes, models, and years of usage~\cite{disk_failure}.
A node exhibiting Byzantine failure can deviate arbitrarily from the protocol, including losing or modifying any data stored locally.
For other types of node failures that are recoverable such as crashes or power failures, we treat them as transient faults.
Permanent node failures are equivalent to nodes leaving the system.

We consider a strong Byzantine fault model in which Byzantine participants can collude with each other and generate arbitrary identities.
We, however, assume a stake-based system, and that the Byzantine participants do not possess more than half of the system stakes in aggregate.
\sgd{Fixing B to 50\%?}
Note that using stake is not an inherent part of the protocol.
\sys only leverages stake to defend against Sybil attacks; other approaches such as proof-of-work or permissioned settings can also be applied.
\sgd{Candidate statement of threat model: no honest node can get compromised after they start to store fragments.}

We also make standard cryptography assumptions, i.e., the probability of subverting cryptographic hash functions or public-key cryptography algorithms we used in the protocol is negligible.


\subsection{Design Overview}

\begin{figure}
    \begin{mdframed}[style=protocol]
        \textbf{\sys node:}
        \begin{itemize}
            \setlength\itemsep{0em}
            \item \textit{N}: total number of nodes
            \item \textit{sk}: secret key of the node
            \item \textit{VRF}: verifiable random function to predicate the set of objects to take care of
            \item \textit{store}: local storage of data fragments, indexed by object ID
            \item \textit{eligible\_objects}: objects' health situation indexed by object ID, for future recovery when the object is about to lose
            \item \sgd{mesh peers for gossip, or alternatively treat gossip protocol as blackbox}
        \end{itemize}
    \end{mdframed}
    \caption{Local state of \sys nodes.}
    \label{fig:node-state}
\end{figure}

\autoref{fig:node-state} shows the local state maintained by each \sys node.
Each node generates a (\textit{sk}, \textit{pk}) key pair, and stores the secret key \textit{sk} locally.
Public keys are assumed to be known by all nodes in the system.
Each node locally stores a set of data fragments generated from fountain code encoding.
They also maintain indices to lookup data fragments from object IDs.

\sys nodes communicate through both point-to-point channels and a gossip protocol.
Point-to-point channels are \textit{reliable}.
They can be implemented using protocols such as TCP and Reliable User Datagram Protocol (RUDP).
The gossip protocol is best-effort and only provides weak guarantees: If a node sends a message through the protocol indefinitely, eventually all non-faulty nodes receive the message.

\sys uses a publicly-known fountain code to encode each stored object into an infinite stream of encoding fragments.
As introduced in \autoref{sec:background:fc}, any $k + \epsilon$ distinct encoding fragments can be used to reconstruct the original object with overwhelmingly high probability.
To guarantee data durability, \sys enforces the following \textit{invariant}: at any time $t$ after a \store is completed, at least $k + \epsilon$ encoding fragments of the object are present in the system.

Storage device failures and nodes leaving the system can lead to permanent loss of encoding fragments, resulting in violation of the durability invariant.
To avoid relying on a centralized trusted service to detect fragment losses, each node periodically broadcasts \textit{persistence proof} of its stored fragments through the gossip protocol.
\lijl{Need to properly define persistence proof. Do we care if a node lie about persistence? Or only rely on collective behavior? Trade-off?}
By monitoring the persistence proofs, \sys nodes can independently generate and store new encoding fragments when the number of remaining fragments is below a threshold.

Naively implementing the above scheme has two issues.
Firstly, tracking and maintaining all persistence proofs on each node scales poorly.
Second, Byzantine nodes can collude and claim persistence of enough fragments for an object, such that less than $k + \epsilon$ fragments are stored on honest nodes.
Once those Byzantine nodes discard their local fragments, the object is permanently lost.

\sys uses verifiable random functions (VRFs) to address both issues.
\sys partitions the lifetime of an object into \textit{epochs}.
In each epoch, a randomly selected set of nodes using VRF are responsible for storing the encoding fragments of an object.
VRF ensures that each node can independently generate a selection proof, the selection probability of a node is proportional to the fraction of stakes it owns, and that the selection proof is publicly verifiable.
By properly setting the desired storage redundancy in an epoch, \sys ensures that more than $k + \epsilon$ fragments are stored on honest nodes with high probability.
Moreover, a node only needs to actively monitor persistence proofs for objects that the node is responsible for in the next epoch, which is a small constant proportional to its stake.

\subsection{Protocol Details}

\textbf{Randomized placement group.} \sys selects a random set of peers to monitor and store a data object.
The set of peers changes from time to time. We call the set of peer are \textit{eligible} for taking care of the object.
Given long enough time, every node will be in the set for at least once.
The network is asynchronous. To avoid peers take charge of the object too early or too late, cause unnecessary redundant storage,
we construct a weak causal dependency among the needs.
We split peers into different ages. A peer of $age$ is eligible only after it has observed at least one peer of $age - 1$ being eligible, i.e., gossip health proof.
A peer becomes not eligible if it observes sufficient number of peers of higher ages are eligible.
At that point, the peer is safe to stop monitor the object and discard the stored fragment.

\textbf{Determine eligibility.} Thanks to VRF, each peer can check for whether it is eligible independently, with transferrable authentication.
Similar to Algorand, peer calls $\{h, \pi\} = VRF_{sk}(H(obj), age)$, interprets the resulting hash $h$ as a floating number between $[0, 1)$, and check whether it falls in the binomial distribution's cumulative probability range $\textbf{Pr}(X \le k')$.
If the check is positive, the peer is eligible to take care of $obj$ for $age$, and the fragment it should store is the $h\ \textrm{mod}\ 2^{n}$-th fragment generated by fountain code.
Any peer receives the health proof can verify the peer's eligibility with $\pi$ and peer's public key.

\textbf{Verify fragments.}
\sgd{Maybe should go into implementation section.}
The fragments received from peers can be individually verified thanks to homomorphic hashing.
Given $H(obj)$, the hash of the $i$-th fragment of $obj$ generated by fountain code can be derived.
Peer rejects the fragment if the hashes mismatch.
To avoid expensive homomorphic hashing operations, peers can do optimistic recovering.
It recovers the data object first, then check the hash of recovered object. If the hash is correct then recovery is successful.
Otherwise, it computes homomorphic hashes for each fragment and filter out incorrect fragments.
Then it fetches for more fragments and try to recover again later.

\subsection{Correctness}
