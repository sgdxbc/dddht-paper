\iffalse
\sgd{Alternative flow for background \& motivation section.}

\paragraph{The demand of \emph{store and forget} service in p2p network.}
Definition: \emph{unattended store}.
Client does not participant in setting up store strategy during storing data.
Client can go offline before retrieving the data and does not need to monitor the data.
Reason: client should not build its confidence of the data persistent in the system on the trust to several specific peers.
In another word, whether some specific peers behave correctly or not should not affect the overall confidence level of the client on that the data will not lose.

\paragraph{Scalability failure of blockchains.}
Full replication does not scale.
Partial replication (arweave) only mitigates the problem: workload still grows linearly according to chain depth.

\paragraph{Indexing systems provide mismatched functions.}
IPFS requires client to pick providers, potentially monitor providers' liveness and re-pick when necessary.
Filecoin picks providers for clients, but require clients to actively challenge periodically.
Trying to \emph{store and forget} in such systems can result in data lose.

\paragraph{Demonstration of data lose in indexing system.}
Randomly pick providers w/ or w/o replication.
Simulated faulty providers.

\paragraph{Goals.}
User interacts the system as a whole, not individual peers.
Durability guarantee that only depends on overall properties (e.g. faulty portion, churn rate), regardless of properties of any individual peer.
Controllable redundancy.

\sgd{Original content below.}
\fi

\paragraph{Problem definition.}
~\sgd{edited}
We consider the problem of designing a \emph{durable} object storage system in a fully \emph{decentralized} environment.
The system provides a simple object store and retrieval interface to end users.
Each object contains a blob of binary data of arbitrary size.
The system provides \emph{durability}: Once an object is successfully stored, the object can be \emph{eventually} recovered with its original content until expiration, even if part of the system fails.
Notice the guarantee is weakened compared to \emph{availability}; the recovery may be lagged by the effects of transient failures or network partitioning, but the data is guaranteed to be recoverable, and the recovery will eventually succeed.

Objects are \emph{immutable}, i.e., users cannot modify an object after it is stored.
The system presents a \emph{flat view} of all objects; it does not offer explicit hierarchies such as those in a file system.
It serves as a base persistent storage layer where higher-level functionalities, such as files and relational databases, can be built atop.

\sgd{Current text not mention the work \emph{archival} at all. Should we?}

\sgd{edited}
We target the \emph{cold storage} layer of the storage stack which emphasizes on \emph{data durability}, \emph{storage capacity}, and \emph{cost}, as opposed to the warm~\cite{f4} or the hot storage~\cite{haystack} layers.
Amazon Glacier~\cite{amazon_glacier,amazon_s3} and Google Coldline~\cite{google_coldline}/Archive~\cite{google_archive} storage are popular production-level systems in this category.
In this scenario, performance and availability are not key design metrics of the system.

\paragraph{The decentralized deployment.}
~\sgd{edited}
Our target deployment environment is a fully \emph{decentralized} and \emph{open} network.
The decentralization focuses on the trust model \ie participants, while do not trust any individual other participant, trust the storage network as a whole (based on some preconditions).
Such system is usually also geologically decentralized, though this property is not directly leveraged and the work does not focus on the challenges caused by it.
The deployment model mirrors existing permissionless blockchain systems such as Bitcoin~\cite{bitcoin} and Ethereum~\cite{ethereum}.
There is no central entity to manage or administer the system.
The network is \emph{permissionless}, i.e., anyone can join the network without any authorization required.

Each participating node has some local storage for storing data, as well as network access to the wide area network.
There is, however, no requirement on the type, speed, or quality of their physical storage or network devices.
Participants can be either \emph{honest} or \emph{adversarial}.
Honest nodes follow the protocol exactly, while adversarial nodes can deviate arbitrarily from the protocol.
\sgd{Honest nodes, though honest, still fails from time to time; noticeably, all nodes eventually fail.}
Adversaries may also collude and attack the system.

\iffalse
Our target environment demands stronger fault tolerance.
Besides adversarial behaviors, a decentralized network can have higher churn rate than that of a centralized setting.
For instance, a recent study of IPFS~\cite{ipfs} reveals that 87.6\% of session uptime is under 8 hours and only 2.5\% of them exceed 24 hours;
another study~\cite{bitcoin_churn} shows similar churn rate in the Bitcoin network.
The storage system needs to ensure data durability even when nodes fail and depart at such high rate.
\fi

\paragraph{The principals of durable storage.}
~\sgd{added}
No matter being centrally managed or decentralized, distributed storage systems more or less work in the similar way to ensure durability.
\sgd{Rephrase the text so we can cite the decentralized storage works here.}
The data objects are encoded into fragments and distributed to different storage nodes, so that that failure of any single storage node would not immediately result in data loss.
Then the system constantly detect for storage failures, and repair the fragments to bring the situation back to the same as before the failure happened.

\sgd{edited}
The simplest encoding is replication, where the encoded fragments are just plain copies of the original object.
However, replication introduces large redundancy, negatively impacting the overall storage efficiency.
To reduce storage redundancy without compromising data reliability, modern storage systems apply erasure coding~\cite{google_storage,was,hitchhiker,xor}.
In particular, maximum distance separable (MDS) codes, e.g., Reed-Solomon code~\cite{reed-solomon}, are a common choice.
% Being centrally managed, the system administrator defines \emph{placement groups}.
Each erasure code strip (or replicas of an object) is mapped to one \emph{placement group}, \ie the group of storage nodes each of which stores a fragment for the data object, defined either by the system administrator in a centrally managed storage or automatically by protocol in a decentralized storage.
% To reduce the risk of data losses, placement groups are chosen to minimize correlated failures, where factors such as failure domains and annualized failure rate (AFR) of the storage devices~\cite{pacemaker,tiger} are usually considered.
The placement groups are expected to have minimal correlations, to avoid data loss because of the correlated failures within placement groups.

\paragraph{Correlated failures in decentralized systems.}
~\sgd{Consider move the text from next section to here.}

\paragraph{\textbf{State of the art decentralized storage.}}
~\sgd{edited}
With the popularization of peer-to-peer file sharing networks in the late 90s, designing storage and file systems from a network of decentralized, untrusted computers was an active research topic at the time.
Representative work during this period of time include xFS~\cite{xfs}, Farsite~\cite{farsite}, and OceanStore~\cite{oceanstore}.
Departing from centralized deployment, handling unreliable and malicious participants is a key design goal.
% To provide data availability and consistency in the presence of Byzantine failures, they apply BFT protocols~\cite{pbft} to replicate file data and metadata to groups of servers.
% BFT protocols, however, require less than $1/3$ of the replication group to be Byzantine faulty.
% These solutions often sidestep the issue and sometimes rely on centralized, trusted certification authorities~\cite{farsite}, equivalent to a permissioned setting.
Limited by the hardware resources, those works focused on extending capabilities of single machine while still maintaining adequate durability.
The trend was quickly superseded by the centrally managed solutions as part of the emergence of cloud services.

Recently, there has been renewed interest in decentralized storage, following the success of blockchain systems such as Bitcoin~\cite{bitcoin} and Ethereum~\cite{ethereum}.
\sgd{We don't need datacenters for providing serious services; ordinary personal devices are getting really powerful.}
One recent successful example is IPFS~\cite{ipfs}, a widely deployed decentralized storage network that consists of more than 400 thousand peers globally.
IPFS divides user files into uniquely identified chunks.
File metadata is represented as a Merkle Directed Acyclic Graph (DAG)~\cite{merkle_dag} constructed from the constituent file chunks.
IPFS uses content addressing through Kademlia~\cite{kademlia}, a distributed hash table (DHT)~\cite{chash,chord}.
By writing to the DHT, peers storing a file chunk announce a publisher record which maps the chunk to the peer identifier.
The DHT replicates the publisher record to peers in proximity in the hash space and handles peer failures through gossip.
Retrieving a file involves querying the DHT for the publisher records and the address of the publishers for each file chunk.
IPFS, however, does not offer any file durability guarantee.
File content in IPFS is only stored on the original file publishers;
the protocol has no explicit redundancy mechanism to enforce file content durability.
\sgd{In another word, it relies on external services for dealing with placement groups, and eventually providing durability.}

\paragraph{Failure correlations are not tackled in a systematical way.}
~\sgd{added}
For the decentralized storage that claims to provide durability, the unique challenging characteristics of correlated failures in decentralized systems are not specially handled.
The DHT-based works distribute fragments in a static pattern \eg evenly spaced on the ring of nodes, clustered among the nodes whose IDs are the closest to the content hash, etc.
\sgd{TODO cite}
The object level randomness is deployed but mostly for load balancing.
Essentially, those works \emph{assume} there's no correlation among the distributed nodes without any evidence.
\sgd{TODO preview the simulation results}

\sgd{There seems to be some works considering geological information, but that was for fast routing i remembered?}

\sgd{added}
On the other hand, Filecoin~\cite{filecoin} and Arweave~\cite{ar} both use tokens to incentivize nodes to store objects longer, expecting to overcome the correlated failures by minimizing the overall failure rate.
The cost is eventually passed on to the end user, resulting in less attractive expensive storage services.
And what's worse, the probabilities of unintentional correlated failures caused by \eg power outage, nature disasters, etc. will not decrease according to the incentive.
The systems actually relies on individual storage nodes to bring them own solutions to tolerate these correlated failures, which are usually centrally managed.
The defeats the purpose of extending durability with decentralization; users are essentially trusting the fault tolerance solutions of the individual nodes that store them data.

\paragraph{Methodology of designing durable decentralized storage.}
~\sgd{added}
Motivated by the overlook of correlated failures of the previous works, we propose not only a storage system that we believe to be significantly more durable, but also a whole framework for designing and reasoning the durability of storage systems in the presence of unpredictable correlated failures.
In \autoref{sec:model}, we formalize the unpredictability into a fault model and perform several analyses to previous solutions with it.
Which is followed by \autoref{sec:protocol}, where we propose a reference design that can ensure durability with bounded redundancy according to the fault model.
The decentralized system is the most dangerous place for data because of its unpredictable correlations; however, as soon as we make proper design against it, the decentralized nature will instead make it the safest, and we would be granted with durability for the least trust ever.

\iffalse
\begin{outline}
% \sgd{Most desired previous works are already here, just listing the expected flow.}
% \lijl{Merge background and related work in this section.}

% \lijl{We could give durability definition first, before introducing prior work.}
% \sgd{Definition is given in the first paragraph.}

% First are the 20 years ago works that designed for extending single node's capacity.
% Either centrally managed or not, as that was not the key concern.
% While the unmanaged solutions had the opportunity of achieving supreme durability, their design actually cannot.
% Later analysis prove this.

% In the next period most works are centrally managed.
% Their durability are bounded by the managing organization, as stated before.

% Recent works start to revisit unmanaged solutions mostly for web3 uses.
% Most noticeable ones are blockchains and Filecoin.

% Blockchain bypasses correlated failures by overwhelming redundancy.
% It's not a good fit for storage system.
% And it is not meant for storage.

% \lijl{Probably don't need to say too much about blockchains, since they are not storage systems.
% Focus on IPFS, Filecoin, and AR.}

% Filecoin and other pure incentive solutions try to bypass correlated failures with extremely low failure rates.
% That is not solving the problem.
% Also, that is not a sound solution, since failure rates cannot be reduced to zero in this way.

% \sgd{The following need to be appended to existing material.}
% As a conclusion, current decentralized storage systems overlook correlated failures.
% \lijl{Should discuss correlated failures first, probably in the beginning of the section, before introducing prior work.}

% Correlated failures in a decentralized system deserve more serious consideration.
% Unlike managed infrastructures, decentralized system failures may exploit arbitrary correlations.
% There's no way to study the correlations in a permissionless system in a reliable and trusted way.
% Correlations in the wild are multidimensional.
% In datacenters storage nodes are carefully deployed to reduce the dimensions.
% This is impossible in permissionless systems.
% \lijl{We need concrete evidences to support these claims.
% We should check if there is any prior work that measure and characterize failures, particularly correlated failures, in decentralized systems.
% }

All the previous works tempt to \emph{mitigate} the unknown failure domains.
Try to convert them into predetermined ones, or just pretend they are predetermined.
In another word they are designing decentralized storage in the same way of designing centrally managed storage.
Use extra measurement to polyfill the difference between the deployed environments.

If we want to go beyond what durability centrally managed systems can provide with decentralized solution, we have to go another way.
We will first model the correlated failures of our deploying environment, then design a system that \emph{natively} fit the model.
\lijl{I assume this part is to transition to the next section.}
\end{outline}

\paragraph{\textbf{Prior distributed storage systems.}}
There is a long line of work on centrally managed distributed storage systems~\cite{ceph,gfs,bigtable,dynamo,liquid}.
These systems commonly divide objects into equal-sized chunks, and deploy a large pool of nodes to store those chunks.
A dedicated set of nodes are responsible for maintaining and processing storage metadata.
It is also common to use a centralized consensus service such as Chubby~\cite{chubby} to manage configurations and other critical system states.

At the deployment scale of these systems, failures of storage devices, servers, racks, clusters, and even entire data centers are frequent events.
To guarantee data durability and availability, replication techniques such as primary-backup~\cite{pb} and state machine replication~\cite{smr} are commonly deployed.
However, replication introduces large redundancy, negatively impacting the overall storage efficiency.
To reduce storage redundancy without compromising data reliability, modern storage systems apply erasure coding~\cite{google_storage,was,hitchhiker,xor}.
In particular, maximum distance separable (MDS) codes, e.g., Reed-Solomon code~\cite{reed-solomon}, are a common choice.
Being centrally managed, the system administrator defines \emph{placement groups}.
Each erasure code strip (or replicas of an object) is mapped to one placement group.
To reduce the risk of data losses, placement groups are chosen to minimize correlated failures, where factors such as failure domains and annualized failure rate (AFR) of the storage devices~\cite{pacemaker,tiger} are usually considered.

These systems, however, only consider crash failures of individual components.
All participants of the system are under a single, protected administrative domain, so there is no requirement to tolerate Byzantine failures~\cite{bft}.

\paragraph{\textbf{State of the art decentralized storage.}}
With the popularization of peer-to-peer file sharing networks in the late 90s, designing storage and file systems from a network of decentralized, untrusted computers was an active research topic at the time.
Representative work during this period of time include xFS~\cite{xfs}, Farsite~\cite{farsite}, and OceanStore~\cite{oceanstore}.
Departing from centralized deployment, handling unreliable and malicious participants is a key design goal.
To provide data availability and consistency in the presence of Byzantine failures, they apply BFT protocols~\cite{pbft} to replicate file data and metadata to groups of servers.
BFT protocols, however, require less than $1/3$ of the replication group to be Byzantine faulty.
These solutions often sidestep the issue and sometimes rely on centralized, trusted certification authorities~\cite{farsite}, equivalent to a permissioned setting.

Recently, there has been renewed interest in decentralized storage, following the success of blockchain systems such as Bitcoin~\cite{bitcoin} and Ethereum~\cite{ethereum}.
One recent successful example is IPFS~\cite{ipfs}, a widely deployed decentralized storage network that consists of more than 400 thousand peers globally.
IPFS divides user files into uniquely identified chunks.
File metadata is represented as a Merkle Directed Acyclic Graph (DAG)~\cite{merkle_dag} constructed from the constituent file chunks.
IPFS uses content addressing through Kademlia~\cite{kademlia}, a distributed hash table (DHT)~\cite{chash,chord}.
By writing to the DHT, peers storing a file chunk announce a publisher record which maps the chunk to the peer identifier.
The DHT replicates the publisher record to peers in proximity in the hash space and handles peer failures through gossip.
Retrieving a file involves querying the DHT for the publisher records and the address of the publishers for each file chunk.
IPFS, however, does not offer any file durability guarantee.
File content in IPFS is only stored on the original file publishers;
the protocol has no explicit redundancy mechanism to enforce file content durability.

\iffalse
\paragraph{\textbf{IPFS case study.}}
To investigate the durability guarantees in existing systems, we conducted a measurement study on the public IPFS network.
\lijl{Describe study and results.}
\fi

\ifspace
Filecoin~\cite{filecoin} addresses this issue using an incentive-based approach called Proof-of-Storage~\cite{fc_pos}.
To store a file chunk, peers deposit tokens to the network.
During the lifetime of the chunk, other peers in the network issue cryptographic challenges to the node prove its storage of the chunk content.
The challenge can only be solved if the chunk is stored for the time duration.
Unsuccessful proofs result in slashing of the token deposit, disincentivizing nodes to lose stored data.
\fi
\fi
